[kafka_source]
bootstrap_servers = ${KAFKA_SOURCE_BOOTSTRAP_SERVERS}
security_protocol = ${KAFKA_SOURCE_SECURITY_PROTOCOL}
sasl_mechanism = ${KAFKA_SOURCE_SASL_MECHANISM}
sasl_plain_username = ${KAFKA_SOURCE_USERNAME}
sasl_plain_password = ${KAFKA_SOURCE_PASSWORD}
topic_source = ${KAFKA_SOURCE_TOPIC}

[kafka_local]
bootstrap_servers = ${KAFKA_LOCAL_BOOTSTRAP_SERVERS}
security_protocol = ${KAFKA_LOCAL_SECURITY_PROTOCOL}
sasl_mechanism = ${KAFKA_LOCAL_SASL_MECHANISM}
sasl_plain_username = ${KAFKA_LOCAL_USERNAME}
sasl_plain_password = ${KAFKA_LOCAL_PASSWORD}
charge_succeeded = ${KAFKA_LOCAL_TOPIC_STRIPE}
topic_user_loggedin = ${KAFKA_LOCAL_TOPIC_USER_LOGGEDIN}
topic_user_loggedout = ${KAFKA_LOCAL_TOPIC_USER_LOGGEDOUT}
topic_course_viewed = ${KAFKA_LOCAL_TOPIC_COURSE_VIEWED}
topic_discussion_viewed = ${KAFKA_LOCAL_TOPIC_DISCUSSION_VIEWED}
topic_error = ${KAFKA_LOCAL_TOPIC_ERROR}
